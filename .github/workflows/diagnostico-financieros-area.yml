name: Diagnóstico de financieros aislados por área

# Consulta puntual, solo lectura: busca archivos financieros y de balance
# fiduciario recientes, y el estado actual de cada raíz financiera en
# live_data_points, para confirmar si el bug de área (corregido en
# updateIsAutoPublishable) dejó algo aislado en silencio antes de la
# corrección. Se retira tras el diagnóstico.

on:
  workflow_dispatch: {}

jobs:
  diagnosticar:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - name: Diagnosticar
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: node scripts/diagnostico-financieros-area.mjs
